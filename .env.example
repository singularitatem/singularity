# Required for the default `PROVIDER=chai` dev flow.
# Leave this blank in version control and set it in `.env` locally.
# If `PROVIDER=chai` and `CHAI_API_KEY` is missing, `make dev` will fail
# immediately with a clear backend startup error.
PROVIDER=chai
CHAI_API_KEY=
CHAI_USER_NAME=User
