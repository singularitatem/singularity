# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# AI provider: "chai" (default) or "echo" (offline dev, no key needed)
PROVIDER=chai

# Chai inference API key — get one at https://chai.ml
CHAI_API_KEY=your_key_here
